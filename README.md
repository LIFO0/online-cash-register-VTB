# Онлайн-касса ВТБ

Банковское веб-приложение на Django для управления счетами и транзакциями.

## Возможности

- 🔐 Авторизация пользователей (клиенты и администраторы)
- 💰 Управление банковскими счетами
- 📊 Финансовые операции:
  - Пополнение счёта
  - Снятие средств
  - Переводы между счетами
- 📋 История транзакций
- 🛡️ Блокировка/разблокировка счетов (для администраторов)
- ❌ Отмена транзакций (для администраторов)

## Быстрый старт

### Требования

- Python 3.12+
- pip

### Установка

1. Клонируйте репозиторий или перейдите в папку проекта:
```bash
cd OnlineCash  # или название вашей директории проекта
```

2. Создайте виртуальное окружение (если ещё не создано):
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:
```bash
# Linux/Mac
source venv/bin/activate

# Windows
venv\Scripts\activate
```

4. Установите зависимости:
```bash
# Для запуска приложения
pip install -r requirements.txt

# Для разработки (включает линтер flake8)
pip install -r requirements-dev.txt
```

5. Примените миграции (создайте таблицы в базе данных):
```bash
python manage.py migrate
```

6. Загрузите тестовые данные (создаст тестовых пользователей и транзакции):
```bash
python manage.py load_test_data
```

   Эта команда создаст:
   - Администратора: `admin` / `Admin@1234`
   - Клиентов: `client1`, `client2` / `Client@1234`
   - 50 дополнительных учетных записей с русскими именами
   - Историю транзакций для всех счетов

7. Запустите сервер разработки:
```bash
python manage.py runserver
```

   Или используйте скрипт быстрого запуска:
   ```bash
   # Linux/Mac
   ./run_project.sh
   
   # Windows
   run_project.bat
   ```

8. Откройте браузер и перейдите по адресу: http://127.0.0.1:8000/

## Тестовые аккаунты

После выполнения `python manage.py load_test_data` будут созданы следующие пользователи:

### Администратор
- **Логин:** `admin`
- **Пароль:** `Admin@1234`
- **Доступ:** Админ-панель + все функции администратора
- **Возможности:** Просмотр всех клиентов, блокировка счетов, отмена транзакций

### Клиенты
- **Логин:** `client1` / **Пароль:** `Client@1234`
- **Логин:** `client2` / **Пароль:** `Client@1234`
- **Доступ:** Личный кабинет клиента
- **Возможности:** Пополнение, снятие, переводы, просмотр истории транзакций

> **Примечание:** Команда `load_test_data` также создает 50 дополнительных учетных записей с русскими именами для более реалистичного тестирования. Для генерации большего количества используйте `python manage.py generate_accounts --count N`.

## Структура проекта

```
OnlineCash/  # Корневая директория проекта (может называться по-другому)
├── config/                          # Настройки проекта Django
│   ├── settings.py                  # Конфигурация приложения
│   ├── urls.py                      # Главные URL-маршруты
│   ├── wsgi.py                      # WSGI конфигурация
│   └── asgi.py                      # ASGI конфигурация
├── banking/                         # Основное приложение
│   ├── models.py                    # Модели данных (ClientProfile, Account, Transaction)
│   ├── views.py                     # Обработчики запросов (Views)
│   ├── forms.py                     # Формы (DepositForm, WithdrawalForm, TransferForm)
│   ├── services.py                  # Бизнес-логика транзакций
│   ├── urls.py                      # URL-маршруты приложения
│   ├── admin.py                     # Конфигурация админ-панели
│   ├── utils.py                     # Утилиты (нормализация текста)
│   ├── apps.py                      # Конфигурация приложения
│   ├── tests/                       # Папка с тестами (организована по модулям)
│   │   ├── __init__.py              # Инициализация тестов
│   │   ├── test_models.py           # Тесты моделей
│   │   ├── test_views.py            # Тесты представлений
│   │   ├── test_services.py         # Тесты сервисов
│   │   └── test_forms.py            # Тесты форм
│   ├── management/
│   │   └── commands/                # Management команды
│   │       ├── load_test_data.py    # Загрузка тестовых данных
│   │       ├── generate_accounts.py # Генерация учетных записей
│   │       └── randomize_transaction_dates.py  # Рандомизация дат транзакций
│   └── migrations/                  # Миграции базы данных
├── templates/                       # HTML-шаблоны
│   └── banking/
│       ├── base.html                # Базовый шаблон
│       ├── login.html               # Страница входа
│       ├── client_dashboard.html    # Панель клиента
│       ├── transaction_receipt.html # Чек транзакции
│       └── admin/
│           └── dashboard.html       # Админ-панель
├── static/                          # Статические файлы
│   ├── css/                         # Стили
│   │   ├── admin_dashboard.css      # Стили админ-панели
│   │   ├── client_dashboard.css     # Стили панели клиента
│   │   ├── login.css                # Стили страницы входа
│   │   └── styles.css               # Общие стили
│   └── images/                      # Изображения
├── manage.py                        # Утилита управления Django
├── requirements.txt                 # Зависимости проекта
├── requirements-dev.txt             # Зависимости для разработки (flake8)
├── .flake8                          # Конфигурация линтера flake8
├── run_project.sh                   # Скрипт запуска (Linux/Mac)
└── run_project.bat                  # Скрипт запуска (Windows)
```

## Документация

📖 **Подробная документация для изучения Django:** [DOCUMENTATION.md](DOCUMENTATION.md)

Документация объясняет:
- Что такое Django и как он работает
- Структуру проекта
- Модели, Views, Templates, Forms
- Как всё работает вместе
- Примеры кода с объяснениями

**Рекомендуется к прочтению**, если вы изучаете Django!

## Тестирование

Проект включает набор тестов для проверки функциональности:

```bash
# Запуск всех тестов
python manage.py test banking

# Запуск тестов из конкретного модуля
python manage.py test banking.tests.test_models
python manage.py test banking.tests.test_views
python manage.py test banking.tests.test_services
python manage.py test banking.tests.test_forms

# Запуск конкретного теста
python manage.py test banking.tests.test_models.ClientProfileModelTests
python manage.py test banking.tests.test_views.ClientDashboardViewTests
```

**Важно:** Все тесты выполняются в отдельной тестовой базе данных, которая создается перед запуском и автоматически удаляется после завершения. Ваша основная база данных (`db.sqlite3`) не изменяется при запуске тестов.

Тесты покрывают:
- **Модели** (`test_models.py`):
  - Свойства и методы моделей (ClientProfile, Account, Transaction)
  - Генерацию уникальных reference
  - Нормализацию текста (исправление mojibake)
  - Связи между транзакциями
- **Представления** (`test_views.py`):
  - Аутентификацию и авторизацию
  - Редиректы для разных типов пользователей
  - Контекст шаблонов
  - Обработку форм
  - Пагинацию и фильтрацию
- **Сервисы** (`test_services.py`):
  - Создание и обработку транзакций
  - Переводы между счетами
  - Отмену транзакций
  - Блокировку/разблокировку счетов
  - Граничные случаи (лимиты, недостаток средств)
- **Формы** (`test_forms.py`):
  - Валидацию всех форм
  - Проверку лимитов
  - Нормализацию данных
  - Обработку ошибок

Всего в проекте **126 тестов**, которые проверяют основную функциональность и граничные случаи.

## Основные команды

### Управление сервером

```bash
# Запуск сервера разработки
python manage.py runserver

# Запуск на определенном порту
python manage.py runserver 8080
```

### Управление базой данных

```bash
# Создание миграций (после изменения models.py)
python manage.py makemigrations

# Применение миграций
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser
```

### Тестовые данные и генерация

```bash
# Загрузка тестовых данных (создает admin, client1, client2 + 50 дополнительных аккаунтов)
python manage.py load_test_data

# Генерация дополнительных учетных записей
python manage.py generate_accounts
python manage.py generate_accounts --count 100  # Указать количество

# Рандомизация дат транзакций
python manage.py randomize_transaction_dates
python manage.py randomize_transaction_dates --months-back 6  # За последние 6 месяцев
```

### Разработка и отладка

```bash
# Проверка проекта на ошибки
python manage.py check

# Django shell (интерактивная консоль)
python manage.py shell

# Запуск тестов
python manage.py test banking

# Проверка кода линтером flake8
flake8 .
```

### Быстрый запуск

Для автоматической настройки и запуска используйте скрипты:

```bash
# Linux/Mac
./run_project.sh

# Windows
run_project.bat
```

Эти скрипты автоматически:
- Создают виртуальное окружение (если нужно)
- Устанавливают зависимости
- Применяют миграции
- Загружают тестовые данные
- Запускают сервер

## Технологии

- **Django 5.2.8** - веб-фреймворк для Python
- **SQLite** - база данных (по умолчанию, легко заменяется на PostgreSQL/MySQL)
- **Python 3.12+** - язык программирования
- **HTML/CSS** - фронтенд (шаблоны Django + кастомные стили)
- **tzdata 2024.2** - данные о часовых поясах
- **flake8 7.3.0** - линтер для проверки качества кода (для разработки)

## Функциональность

### Для клиентов
- ✅ Авторизация в системе
- ✅ Просмотр баланса счета
- ✅ Пополнение счета с комментарием
- ✅ Снятие средств с комментарием
- ✅ Переводы между счетами
- ✅ Просмотр истории транзакций (последние 10)
- ✅ Просмотр чека транзакции

### Для администраторов
- ✅ Авторизация в системе
- ✅ Просмотр всех клиентов с фильтрацией и поиском
- ✅ Пагинация списка клиентов (12 на страницу)
- ✅ Просмотр всех транзакций с фильтрацией
- ✅ Блокировка/разблокировка счетов
- ✅ Отмена транзакций
- ✅ Просмотр общей статистики (баланс, количество клиентов, транзакций)

## Лицензия

Этот проект создан в образовательных целях.

